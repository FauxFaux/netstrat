[package]
name = "netzact"
version = "0.1.0"
authors = ["Chris West (Faux) <git@goeswhere.com>"]

[dependencies]
cast = "0.2"
error-chain = "0.11"
libc = "0.2"
nix = "0.10"

[profile.release]
lto = true

[dependencies.nom]
version = "3"
features = ["nightly", "verbose-errors"]
